# Todolist — 25 July 2026

## Carried over from todolist_23Jul2026.md

No `todolist_24Jul2026.md` was written — 23 Jul's "Tomorrow (24 Jul 2026) — priority order" section carried forward as-is into today's session start.

1. ~~JD Portal v2 Phase C — step-wizard redesign (Configure/JD Run/Reports) + light/dark theme toggle~~ — **done, verified 25 Jul 2026.** See `docs/guides/JDPORTALV2PHASEC_25JUL2026.md`.
2. **Dynamic width further enhancement** (per-breakpoint gutter/max-width values) — **still outstanding.** Was flagged as a candidate to fold into Phase C since both touch `PortalShell.css`, but Phase C's scoping (confirmed with the user this session) was strictly Configure/JD Run/Reports step mapping, History placement, and the theme toggle — width tuning wasn't part of the confirmed scope, so it stayed out. Carry forward, medium priority.
3. **Remaining JD Automation Portal phases** (NLP `update_profile_json`, integration, Docker packaging, dev-env docs, VPS deploy) — still outstanding. Deploy phase covers `/api/auth/*`, `/api/view/*`, `/api/settings/*` routes and both `secrets/jd_portal_auth.json` + `secrets/jd_portal_llm_keys.json` provisioning on the VPS.
4. **LinkedIn job-search automation scoping** — not started.

## New today (25 Jul 2026) — JD Portal v2 Phase C

Session opened with soul.md intake (read latest todolist + status docs), then scoped Phase C via clarifying questions before implementation, per soul.md and explicit user request:

- **Step mapping confirmed:** Configure = JD paste only; JD Run = CLI params + command preview + run/progress/force-stop; Reports = finished scorecard/resume/cover-letter output.
- **History placement confirmed:** stays a separate top-level tab, untouched by the wizard restructure.
- **Theme toggle confirmed:** lives in `PortalShell`'s header, defaults to the existing dark ("fintech") theme, new bespoke light palette is opt-in.

Built and verified end-to-end (real Playwright run against the live dev stack, including a genuine `--scorecard-only` job). Full record: `docs/guides/JDPORTALV2PHASEC_25JUL2026.md`.

Also committed two items left uncommitted at the end of the 23 Jul session (hiring-manager explainer deck v2 + Live Demo slides, and their status-doc/data-file companions) before starting today's work.

## New today (25 Jul 2026, later same day) — Phase C follow-up: textarea UX + pipeline retry fix

User exercised the new wizard for a real run and reported two issues, logged here per soul.md intake workflow:

1. **Configure step's JD-paste textarea too small** — a leftover from when it shared the screen with the Run panel below it. **Fixed 25 Jul 2026** — `min-height` raised 140px → 360px desktop (responsive: 260px/200px at 768px/480px). See `docs/guides/JDPORTALPHASECFOLLOWUP_25JUL2026.md`.
2. **Real run failed: "JD pipeline exited with a non-zero status"** (Manulife JD, `--refresh-blueprint --ResumeAdjustment --llm=sonnet`, mode=all). **Root-caused and fixed 25 Jul 2026** — a transient 403 from OpenRouter's multi-provider routing (confirmed: same key/model worked seconds later; not an invalid key, not a wizard bug). `call_llm()` in `scripts/jd_scorecard_resume_v2.py` had zero retry logic — a single transient error killed the whole 5-call pipeline. Added retry-with-backoff (3 attempts, linear backoff) on connection errors and `{403,408,425,429,500,502,503,504}`. Re-ran the exact failing scenario end-to-end — succeeded, all 6 output files written. Full record: `docs/guides/JDPORTALPHASECFOLLOWUP_25JUL2026.md`.
   - **Separate finding, resolved:** a stale Windows User-level `OPENROUTER_API_KEY` env var (dead key, different from the working `.env.local` one) was found during diagnosis — didn't cause this failure but was a latent landmine for anything reading `os.environ` directly. Removed per user decision.

## New backlog items (25 Jul 2026, later still) — reported by user, scoped same day

Three items logged per soul.md intake workflow, then scoped via three parallel investigation passes plus user decisions on the open questions each surfaced. User confirmed these sit **above** the existing carried-forward backlog (dynamic width, remaining portal phases, LinkedIn scoping) in priority order.

1. **Reports step — top of the viewed document was cut off/hidden.** Root-caused and **fixed 25 Jul 2026**: `.jd-portal-card`'s `backdrop-filter` was establishing a new containing block for `DocViewer`'s `position: fixed` modal, breaking it out of full-viewport coverage and clipping content that bled above the card's own (much smaller) box. Fixed by portaling `DocViewer` to `document.body` via `createPortal`, with `data-portal-theme` re-applied on the portaled root since it's now outside `.portal-shell`'s themed subtree. History's `DocViewerInline` (a separate, non-fixed-position component) was confirmed **not** affected by this bug — no code change needed there.
2. **History — ability to delete run files.** **Done, implemented and verified 25 Jul 2026.** Company-level soft-delete (moves `data_processed/<Company>/` to a timestamped `.trash/` folder; source JD text and blueprint cache left alone), inline confirm banner, concurrent-run guard (`409` if a run for that employer is in progress). Also surfaced and fixed a Windows-specific `fs.renameSync` `EPERM` issue (switched to copy-then-delete). Full record: `docs/guides/JDPORTALHISTORYDELETE_25JUL2026.md` (scoping doc: `docs/guides/JDPORTALHISTORYDELETE_SCOPING_25JUL2026.md`).
3. **Ability to update the authoritative `src/data/john_profile.json`** — 4-part epic (manual bullet-add, NLP-assisted resume-diff-merge, versioned restore, diff/compare view). **Scoped, not implemented.** User confirmed: LLM-merged changes require an approval/preview step before writing (never write-then-let-restore-fix-it). Full scoping, including a **security finding** (a live, unauthenticated, broken `/api/consolidate` route already on production that targets this same file — user decided to fold its removal into this epic's design rather than patch separately) and the reconciliation with the older "NLP `update_profile_json`" backlog line: `docs/guides/JOHNPROFILEUPDATE_SCOPING_25JUL2026.md`.

## Profile-update epic — plan drafted, paused for user questions (25 Jul 2026, later still)

Per "proceed to 4 and 5," started a fresh Plan Mode cycle for item 5 right after History delete shipped. Three parallel Explore agents plus direct file reads confirmed the design against the real codebase (not just the earlier scoping doc's guesses):

- **Key discovery:** `backend/backup.js` already implements a working, currently-unused snapshot/restore primitive (`backupProfile()`, `restoreProfile(filename)`, `listBackups()`, `cleanupOldBackups()`) writing to a project-root `backup/` dir — supersedes the scoping doc's speculative `data_processed/profile_history/` idea. The new routes will wire this in directly instead of inventing a new convention.
- Confirmed via three clarifying questions: manual editor gets **full typed form fields for all 13 sections** (not a raw-JSON fallback, including a dynamic key/value sub-editor for `major_achievements`' genuinely irregular per-entry metrics); diff view adds the `diff` npm package for word-level highlighting; `scripts/test_consolidation.js` gets deleted alongside `backend/consolidation.js`/`src/utils/consolidation.js` since it only tests the code being removed.
- Full draft plan (backend routes, `profileOps.js`/`llmClient.js` design, frontend components, verification checklist) written to the Plan Mode plan file (`sprightly-enchanting-hare.md` under the Claude plans directory).
- **Paused before approval at user's request** — user has 1-2 more questions/additions before locking it in. **Not implemented. Continue next session.**

## New today (25 Jul 2026, later still) — three additions to the profile-update epic, reported by user, pending clarification

User reviewed the paused plan (`sprightly-enchanting-hare.md`) and raised three items. Logged per soul.md intake workflow; **none implemented yet — clarifying questions asked before starting**, per soul.md's "ask before implementing when unclear" rule and the user's own explicit request to be asked.

1. **Reports step — top-of-document cut-off, reported again.** The 25 Jul entry above states this was root-caused (`.jd-portal-card`'s `backdrop-filter` breaking `DocViewer`'s `position:fixed` containing block) and fixed by portaling `DocViewer` to `document.body` — confirmed still present in `DocViewer.jsx` on disk. User is now describing the same symptom across all three document types (Resume/Scorecard/Cover Letter). Unclear whether this is (a) a regression, (b) a different surface than the already-fixed modal (e.g. content-level clipping inside `.doc-viewer-content` itself, not the overlay/containing-block bug), or (c) mobile-only. **Needs a fresh repro before any fix** — see questions below.
2. **History — delete granularity too coarse.** The 25 Jul company-level soft-delete (`3392b0e`) moves the entire `data_processed/<Employer>/` folder to trash — but a company can have multiple distinct roles run against it (screenshot: Manulife has 4 runs across 2 roles — `AVP_Technology_Architecture_and_Operations` ×2 dates, `IT_Director_DigitizationAutomation_GWAM` ×2 dates), and deleting the whole company throws away roles the user wants to keep. **Confirmed requirement:** move delete to the individual run/row level — one exact `(employer, roleTag, date)` triple, matching the History list's existing row granularity exactly. Deleting the `IT_Director... 21JUL2026` row must leave the `22JUL2026` run of the same role, and both `AVP_Technology...` runs, untouched.
   - Backend implication: `backend/api/history.js`'s `DELETE /:employer` (whole-folder move) needs a new route (e.g. `DELETE /:employer/:roleTag/:date` or a query-param variant) that moves only the matched files — `ScoreCard/{txt,docx,pdf}`, `resume/{txt,docx,pdf}`, `CoverLetter/{txt,docx,pdf}` — filtered by the same `employer_roleTag_date` suffix `scanHistory()`/`findPaired()` already use to pair files, into a mirrored `.trash/<timestamp>/` structure (not a whole-folder copy). The existing copy-then-delete-with-retry EPERM workaround still applies, just per-file/per-match instead of per-directory.
   - Frontend implication: `JDHistoryList.jsx` needs a delete affordance on each individual run row (not just the company header), with its own confirm-banner instance — the existing company-level delete UI likely gets removed/replaced rather than kept alongside, since row-level delete supersedes the original coarse-grained ask (**needs one more confirm at build time**: keep company-level bulk-delete as well, or replace it entirely with row-level?).
3. **`john_profile.json` population/consolidation overhaul.** Currently `profile.metadata.resume_source` names exactly one file (`JohnHauResume2026_MorganStanley.txt`); the other three historical resumes in `data_raw/resume/txt/` (`JohnHauResume2017.txt`, `JohnHauResume2020.txt`, `JohnHauResume2023.txt`) are not reflected. Confirmed by reading the file: the profile was populated once (timestamp `2026-03-30T06:02:48Z`) by the now-deprecated `backend/consolidation.js` — a naive regex-based extractor (splits lines containing `" - "` or `" at "`, no PDF/DOCX support, no LLM) already flagged for deletion in the paused plan as dead/broken. This explains the user's observation that JD-match scorecard numbers run low: `professional_experience` only holds 5 entries despite 27+ years and multiple employers across the older resumes. User's ask has three parts:
   - Read and consolidate all 4 raw resume files (not just the newest), routing bullets to the correct one of the 13 `profile.*` sections via semantic matching rather than a fixed single source file.
   - Rewrite plain bullets into SMART-form (Specific/Measurable/Achievable/Realistic/Time-bound), bolding key figures — same convention already built into `jd_scorecard_resume_v2.py`'s resume-generation prompts (`**double asterisk**` spans), reused here for the source-of-truth data instead of only at output time.
   - Do a full backfill pass now, since the current gaps are the suspected root cause of low scorecard match numbers.

   This is materially larger than the paused plan's part (b) ("Update from Resume" — one pasted resume, LLM proposes ops, human approves before write): that design assumes a single text blob and a human reviewing every proposal individually. A one-time multi-file backfill producing what could be 50-100+ new entries across 13 sections raises different questions about approval granularity, source-file tracking (`resume_source` is currently a single string), and ordering relative to the still-unapproved plan.

**Answers received (25 Jul 2026, later still):**
- **Item 1 (Reports-view cut-off):** confirmed already fixed — no regression, no action needed. Closed.
- **Item 2 (History):** restated and scoped — see full requirement above (row-level delete, one confirm still open on whether to keep company-level bulk-delete alongside it).
- **Item 3 (backfill), approval granularity:** **bulk auto-write, then human reviews the diff/version-history afterward** — not per-item or per-section approval. Rationale (user's choice): source is John's own real historical resumes, not LLM-invented content, so the risk profile differs from the merge-resume feature's original design; the existing backup-before-write + version-restore/diff-view parts of the paused plan already cover the undo path, so a pre-write gate is redundant here.
- **Item 3, build approach:** **extend the paused "Update from Resume" plan to accept multiple files** (not a one-off throwaway script) — this backfill becomes the first real exercise of that feature once multi-file input + semantic section-routing + SMART-bullet rewriting are added to its design, rather than building a parallel one-time tool.
- **Net effect on the paused plan (`sprightly-enchanting-hare.md`):** part (b) needs its scope widened before the plan is re-locked: (i) accept N files, not one pasted blob; (ii) route each extracted bullet to the correct one of the 13 `profile.*` sections via semantic matching rather than the user always naming the target section; (iii) rewrite bullets into SMART form with bold key figures before proposing; (iv) for this specific backfill invocation, skip the per-proposal approval UI and instead auto-write behind a backup, surfacing the result through the diff/version-history view already designed for parts (c)/(d). Not yet re-drafted into the plan file — next step.

## New today (25 Jul 2026, later still) — built: History row-level delete + profile backfill script (AIA first run)

User redirected priority to "fix the authoritative source of truth first," confirmed the History company-vs-row delete question (**keep both**), and asked for the profile backfill to be built as a reusable, parameterized script rather than waiting on the full paused UI plan. Both built and verified this session (soul.md §3.1).

1. **History row-level delete — done, implemented and verified 25 Jul 2026.** New `DELETE /api/history/:employer/run?date=&roleTag=` route (`backend/api/history.js`) moves only one exact run's files to trash, reusing `scanHistory()`'s existing filename-suffix matching; kept **alongside** the existing company-level bulk-delete per user decision (both routes coexist and were verified independently). Frontend: `JDHistoryList.jsx` gained a per-row delete icon + confirm banner; `jdApi.js` gained `deleteHistoryJob()`. Verified via the real dev stack (MFA-swap login technique, throwaway two-run test company, filesystem + API checks, error-path checks) — see `docs/guides/JDPORTALHISTORYROWDELETE_25JUL2026.md`.
2. **`scripts/update_profile_from_resume.py` — done, first run verified 25 Jul 2026.** New, reusable, CLI-parameterized script (file path(s) as arguments, `--company=`, `--llm=`, `--dry-run`) so it can later be spawned by the JD Portal backend the same way `pythonRunner.js` already spawns `jd_scorecard_resume_v2.py`, once a UI exists — per the user's explicit "should have the ability to specify what files to be updated ... through the JD portal or a script" requirement. Backs up `john_profile.json` before every write (Node-`backup.js`-compatible format), only ever adds (never edits/removes) content, and only accepts facts traceable to the raw source text (anti-hallucination rule + a code-level near-duplicate safety net). Confirmed decisions from earlier in this session applied: bulk auto-write (no per-item approval gate for this backfill), built as the first real exercise of the paused plan's part (b) rather than a disposable one-off. First real run against `data_raw/resume/txt/JohnHauResume2025-AIA.txt`: AIA highlights 11→27, `major_achievements` 29→36, `key_topics_for_qa` 31→43, verified purely additive (full before/after diff confirmed every other profile section byte-identical). One fact flagged for user review (an ambiguous "USD 7.6/6.1" unit in the source resume, not invented by the script — see guide). Full record: `docs/guides/JOHNPROFILEBACKFILL_25JUL2026.md`.
   - **Not yet done:** the other 3 historical resumes (2017/2020/2023) haven't been run through the script yet; `core_competencies` wasn't touched this pass; wiring the script into an actual JD Portal UI is still part of the paused `sprightly-enchanting-hare.md` plan.

## New today (25 Jul 2026, later still) — user follow-up after first backfill + row-delete round

User reviewed the AIA backfill and row-delete results live and raised several points, logged per soul.md intake workflow.

1. **Manual source correction** — user edited `data_raw/resume/txt/JohnHauResume2025-AIA.txt` line 42 directly, clarifying the ambiguous "usd 7.6 down to usd 6.1" figure to "7.6M"/"6.1M". Since `update_profile_from_resume.py` only ever adds new entries (never edits existing ones), the already-written `major_achievements` entry didn't pick this up automatically. **Fixed 25 Jul 2026** — corrected the one field (`cost_savings: "USD 7.6M to USD 6.1M infrastructure cost"`) directly in `john_profile.json`, backed up first (`john_profile.json.20260725_V2.bak`), validated JSON after.
2. **Continued the backfill** — ran `JohnHauResume2020.txt` (Edge Technology Group + Morgan Stanley) and `JohnHauResume2017.txt` (Morgan Stanley + Merrill Lynch) through the script, all 4 already-existing-employer extractions from these 2 files now done and verified. Hit and fixed a real bug during this: `call_llm()`'s `max_tokens=6000` was too small once an employer (Morgan Stanley) accumulated enough existing highlights/achievements that the echoed context + new-content generation needed more room — raised to 12000 after two separate `finish_reason=length` failures. Also hardened the prompt: raw resume text can mention multiple employers in one file, so both the system and user prompts now explicitly instruct the model to extract only content about the target employer and ignore anything about a different one mentioned in the same file (verified: the Edge Technology Group extraction from the 2020 file correctly ignored that file's Morgan Stanley section, and vice versa).
   - One achievement flagged for user review, not silently written: "No.1 Global Trading Application Revamp" (from the 2020/2017 files) may substantively overlap with the already-existing "Cutting-Edge Trading System Implementation" achievement — different numbers cited (multi-billions daily volume vs. US$1M first-year revenue) so it was kept as a separate entry, but worth a human check on whether these describe the same underlying project.
   - **Resolved same session:** user decided to extend the script (create new entries for Siemens/Alco) and to skip the 2023 file (skills-only, already well-covered). New `--create-new-employer` flag added (requires `--company=`, gated behind an explicit opt-in so an unmatched company is still a hard error by default). Ran both: **Siemens H.K. Ltd.** (new entry, `IT support Officer, MIS department`, Apr 1997–Apr 1998, +5 highlights/+1 achievement/+8 topics) and **Alco Plastic Products Ltd** (new entry, `PC support Engineer, EDP team`, Feb 1995–Mar 1997, +5 highlights/+1 achievement/+6 topics) — every fact independently verified line-by-line against the raw source before writing. **All 5 raw resume files now processed** (2023 deliberately skipped per user decision). Final state: 7 `professional_experience` entries, 45 `major_achievements`, 91 `key_topics_for_qa`. `core_competencies` remains untouched (not in scope this round). See updated `docs/guides/JOHNPROFILEBACKFILL_25JUL2026.md`.
3. **History row-level delete — docx concern, investigated, not a bug.** User observed a deleted run's `.txt` file removed from `data_processed/Manulife/resume/txt/` but reported the corresponding `.docx` in `resume/docx/` was not removed. Root-caused by inspecting the actual trash output: two real deletions happened via the live portal — (a) `AVP_Technology_Architecture_and_Operations 23JUL2026`, which correctly moved all 6 files (txt+docx for ScoreCard/resume/CoverLetter) to trash, confirmed in `data_processed/.trash/2026-07-25T11-10-23-899Z/Manulife/`; (b) `IT_Director_DigitizationAutomation_GWAM 21JUL2026`, which moved only 4 files (ScoreCard txt+docx, resume txt, CoverLetter txt) — **no docx was ever generated for that specific historical run's resume/cover-letter in the first place** (confirmed: only a `.txt` existed on disk for that run before the delete, per the original folder listing taken earlier in the session). The delete route is working correctly; the appearance of "docx not deleted" was because the deleted run's docx never existed to begin with. No code change needed.
4. **New backlog item — restore deleted history.** User wants the ability to restore files that were soft-deleted (company-level or row-level) back out of `data_processed/.trash/`. Not scoped yet — natural fit alongside the already-designed-but-unbuilt Version History restore mechanism for `john_profile.json` backups (`backend/backup.js`'s `restoreProfile()`), though History's trash and the profile's `backup/` folder are two separate mechanisms today and a restore feature would need its own design (list `.trash/<timestamp>/<Employer>/` entries, move back to `data_processed/<Employer>/...`, handle the case where a same-named run has since been re-generated).
5. **New backlog item — human-readable view + export of `john_profile.json`.** User wants to view the full profile in human-readable form inside the JD Portal, and download it as a formatted `.docx` or `.txt` (not raw JSON). Not scoped yet — likely reuses `DocViewer`'s render-and-view pattern and the existing txt→docx conversion approach already used for Resume/Cover Letter/Scorecard outputs, but needs a per-section rendering design (13 sections, several with irregular per-entry shapes like `major_achievements`) before implementation.

## New today (25 Jul 2026, later still) — both new backlog items built: restore-from-trash + Profile view/export

Per user request ("update github first and then tackle the two new backlog items next"), pushed the day's work to `origin/main` (3 commits: row-level delete, profile backfill script + data, todolist), then scoped and built both items same session.

**Restore-from-trash — done, implemented and verified 25 Jul 2026.** One clarifying question resolved first: on a restore conflict (a live file already exists where a trashed file would land — e.g. the same JD was re-run after the original delete), the restore is **refused entirely** with a clear list of the conflicting files, never a silent overwrite or partial restore (user decision). New `GET /api/history/trash` (lists `.trash/<timestamp>/<Employer>/` entries) and `POST /api/history/trash/:trashId/:employer/restore` routes reuse the exact trash layout the two existing delete routes already write — no new storage convention. Frontend: new "Deleted Items" collapsible section (`JDHistoryTrash.jsx`) below the main History list. Verified end-to-end with a throwaway test company: normal restore, and the conflict-refusal path (confirmed the live/newer file was never touched and the trash entry survived for manual resolution). Full record: `docs/guides/JDPORTALHISTORYRESTORE_25JUL2026.md`.

**Human-readable Profile view + export — done, implemented and verified 25 Jul 2026.** Deliberately read-only, kept separate from the still-disabled/still-paused "Update Profile from JD" NLP-write stub. New `GET /api/profile-view` (read) and `GET /api/profile-view/export?format=txt|docx` (render all 13 sections to plain text, then — for docx — shell out to the already-existing `scripts/convert_txt_to_docx.py` rather than adding a new Node docx-writing dependency). Hit and fixed a real bug: the docx export crashed with a Windows cp1252 `UnicodeEncodeError` on the Python script's emoji print output — same known issue already documented for `pythonRunner.js`, fixed the same way (`PYTHONIOENCODING`/`PYTHONUTF8` env vars on the spawned process). New "Profile" tab in the portal (`ProfileView.jsx`) renders every section as its own collapsible card. Verified: profile fetch, txt export (52KB+, spot-checked formatting), docx export (367 paragraphs via `python-docx`, bold-run placement confirmed correct). Full record: `docs/guides/JDPORTALPROFILEVIEW_25JUL2026.md`.

**Noted in passing:** during this session's work, `metadata.resume_sources` gained a 4th entry, `JohnHauResumeBofa_Edge.txt`, that wasn't run by Claude — the user exercised `scripts/update_profile_from_resume.py` themselves directly (major_achievements 45→46, key_topics_for_qa 91→99). Confirms the script is usable standalone as intended; no action needed, logged here only for continuity so a future session doesn't mistake it for drift.

## Priority order

1. ~~JD Portal v2 Phase C~~ — **done, verified 25 Jul 2026**
2. ~~Phase C follow-up: textarea UX + pipeline retry fix~~ — **done, verified 25 Jul 2026**
3. ~~Reports step — top of viewed document cut off/hidden~~ — **done, fixed and verified 25 Jul 2026**
4. ~~History — ability to delete run files (`[Company]` level)~~ — **done, implemented and verified 25 Jul 2026.** See `docs/guides/JDPORTALHISTORYDELETE_25JUL2026.md`.
4a. ~~History — row-level delete alongside company-level~~ — **done, implemented and verified 25 Jul 2026.** See `docs/guides/JDPORTALHISTORYROWDELETE_25JUL2026.md`.
5. Authoritative `john_profile.json` update capability — **backfill complete for all 5 raw resume files, done and verified 25 Jul 2026.** 7 `professional_experience` entries (2 newly created: Siemens H.K. Ltd., Alco Plastic Products Ltd.), 45 `major_achievements`, 91 `key_topics_for_qa`. See `docs/guides/JOHNPROFILEBACKFILL_25JUL2026.md`. Two items flagged for human review, not silently resolved: the AIA "USD 7.6M/6.1M" unit fix, and a possible overlap between "No.1 Global Trading Application Revamp" and "Cutting-Edge Trading System Implementation" (both Morgan Stanley achievements). **Still open:** `core_competencies` untouched by any run; the full interactive UI epic (manual editor, in-portal "Update from Resume" with multi-file support, version history, diff view) is still **plan drafted, paused for user questions, not yet approved/implemented** — see `docs/guides/JOHNPROFILEUPDATE_SCOPING_25JUL2026.md` and the "Profile-update epic — plan drafted" section above.
6. ~~Restore deleted History files~~ — **done, implemented and verified 25 Jul 2026.** See `docs/guides/JDPORTALHISTORYRESTORE_25JUL2026.md`.
7. ~~Human-readable view + export of `john_profile.json`~~ — **done, implemented and verified 25 Jul 2026.** See `docs/guides/JDPORTALPROFILEVIEW_25JUL2026.md`.
8. Dynamic width further enhancement (per-breakpoint values) — medium priority, carried forward
9. Remaining JD Automation Portal phases (NLP, integration, Docker, dev env docs, deploy) — **note:** the "NLP `update_profile_json`" part of this item is now superseded by item 5 above; reconcile when this item is next picked up.
10. LinkedIn automation scoping — not started

## New today (25 Jul 2026, end of session) — `jd_scorecard_resume_v2.py` output quality, requested for tomorrow

User manually edited `src/data/john_profile.json` again after the day's backfill work (confirmed: file is valid, matches the last commit, no further action needed tonight). Five follow-up requests logged per soul.md intake workflow — **explicitly deferred to tomorrow (26 Jul 2026), not implemented tonight.** Grounded against the actual current script behavior (read, not assumed) so tomorrow's session can start from confirmed facts rather than re-deriving them:

a. (Context line, not a standalone item) — these changes are driven by `src/data/john_profile.json` now being far richer than when `jd_scorecard_resume_v2.py`'s prompts were last tuned (7 `professional_experience` entries vs. 5, 46 `major_achievements` vs. 29, 99 `key_topics_for_qa` vs. 31 at the start of today).
b. **Resume/cover-letter generation should draw on all 13 `profile.*` sections, including `linkedin_recommendations`.** Confirmed by reading `build_profile_context()` (`scripts/jd_scorecard_resume_v2.py:546`): it currently includes only 9 of 13 — `metadata`, `summary`, `professional_experience`, `major_achievements`, `ai_projects`, `core_competencies`, `technical_skills`, `education_certifications`, `languages_spoken`. Missing: `linkedin_recommendations`, `soft_skills`, `languages`, `key_topics_for_qa`. **Real technical risk to plan around:** the function truncates its output to `[:14000]` characters — adding 4 more sections (especially `linkedin_recommendations`, which has 16 full-text entries) on top of a profile that's already grown substantially risks silently truncating something else instead. Raising the cap and/or building the context more selectively (e.g. summarizing/trimming less-critical sections rather than a hard char-count truncation) needs to be part of this change, not just adding the missing keys.
c. **Bullets should be ordered by importance/impact, SMART, most impactful first.** SMART-form wording + bolded figures is already an existing system rule (rule 8 in `RESUME_SYS`), but there is currently no instruction governing bullet *order* within a role — today's prompt just says "choose the bullets most relevant to the JD" (JD-relevance selection) without an explicit importance-ranking/ordering rule. Needs a new instruction: within each role, lead with the highest-impact/most quantified bullets.
d. **Roles ordered latest-and-biggest-first (example given: AIA, Bank of America, Edge, Morgan Stanley, Merrill Lynch).** Checked `profile.professional_experience`'s actual current array order — it already is exactly AIA → Bank of America → Edge Technology Group → Morgan Stanley → Merrill Lynch → Siemens H.K. Ltd. → Alco Plastic Products Ltd., i.e. already latest-role-first (and, per the user's stated reasoning, biggest-remit-first happens to coincide with reverse-chronological order for this specific career history). The **data** already satisfies the request; what's unconfirmed is whether the **LLM's output text** reliably preserves that same order when it writes the resume (nothing in the current prompt explicitly instructs it to preserve the profile's role order) — needs an explicit ordering instruction added defensively, and a real generated resume checked to confirm the output sequence matches, not just trusted implicitly. Also note: the `BULLET COUNT RULES` block (`RESUME_USER`, ~line 1022) hardcodes exactly 5 named company tiers (12/12/10/10/8 bullets) plus a combined "earlier roles: 3-4 bullets" catch-all — with 7 companies now, Siemens and Alco both fall into that catch-all bucket, which still works but is worth confirming reads correctly with 2 "earlier roles" instead of a hypothetical 1.
e. **Update the major achievements handling for the new data volume.** Interpreted as: with `major_achievements` now at 46 entries (up from 29 this morning), the resume's achievements content needs a relevance/impact-based selection per JD (echoing item c's "top importance" principle applied specifically to this section) rather than an unfiltered dump — **this interpretation needs a quick confirmation at the start of tomorrow's session** before implementing, since "update the major achievement section based on the new updates" could also plausibly mean reviewing/cleaning the raw JSON data itself rather than changing how the generation script selects from it. Flagged rather than guessed.

## New today (25 Jul 2026, later still) — real bug fixed live: portal login "Failed to fetch"

User hit a real, live-blocking bug: `http://localhost:5175/portal/enroll` showed "First-time Portal Setup" with a `Failed to fetch` error, even though the account was already enrolled (since 22 Jul 2026). **Root-caused and fixed 25 Jul 2026.**

**Root cause:** `backend/jd_api_server.js`'s CORS config hardcoded a single allowed origin (`http://localhost:5173`). Across the day's session, multiple Vite dev-server processes had accumulated without being cleanly stopped (confirmed 3 separate `vite.js` processes running simultaneously on ports 5173/5174/5175, hours apart in start time) — the user's active tab had landed on port 5175 once 5173/5174 were left occupied by orphaned processes, which wasn't in the CORS allow-list. Confirmed via a direct `curl -X OPTIONS` preflight simulation: the server replied with the wrong `Access-Control-Allow-Origin`, which the browser correctly refuses to expose to `fetch()` — surfacing as a generic, undiagnostic `Failed to fetch`.

**Compounding factor, documented not fixed:** `PortalEnroll.jsx`'s enrollment-status check silently fails open to showing the enroll form on any fetch error (no visible error, no distinction between "genuinely not enrolled" and "couldn't check") — this is why a connectivity failure looked exactly like a fresh, un-enrolled portal. Flagged as a good small hardening for a future pass (show the actual error + a manual retry/go-to-login action) — not built tonight, out of scope for "fix the login."

**Fix:** CORS now validates against a configurable, comma-separated allow-list (`JD_PORTAL_FRONTEND_ORIGIN`) instead of one hardcoded origin, defaulting to the common Vite port range (5173-5176) if unset; `.env.local` updated to list all 4. Cleaned up the two orphaned Vite processes, restarted the backend to load the new config. Verified: preflight from origin 5175 now succeeds, `GET /api/auth/status` confirms `enrolled:true`, `npm run build` clean. No changes to `secrets/jd_portal_auth.json` — existing enrollment/password untouched. Full record: `docs/guides/JDPORTALCORSFIX_25JUL2026.md`.

**User action needed:** refresh the portal tab (same URL, no change needed) — should auto-redirect to `/portal/login`. Asked the user whether they remember the existing password or want a full reset (Method B in `docs/guides/JDPORTALPASSWORDROTATION_25JUL2026.md`).

**Scope note:** none of a-e were implemented tonight (explicit user instruction: "leave those to be done by tomorrow"). No code changes made to `jd_scorecard_resume_v2.py` this session beyond what's already committed (the v1→v2 script itself was untouched today).
